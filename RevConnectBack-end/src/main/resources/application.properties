spring.application.name=RevConnect

spring.datasource.url=jdbc:mysql://localhost:3306/skproject?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=2725
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=9999

jwt.secret=VGhpc0lzQVN1cGVyU2VjcmV0S2V5Rm9ySldUMTIzNDU2Nzg5MA==
jwt.expiration=3600000

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html

# Mail configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:revconnect10@gmail.com}
spring.mail.password=${MAIL_PASSWORD:bcomkpazedxcanrl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

app.mail.from=${MAIL_FROM:${spring.mail.username}}
app.mail.enable-styles=${MAIL_ENABLE_STYLES:true}
app.mail.brand.name=${MAIL_BRAND_NAME:RevConnect}
app.mail.brand.primary-color=${MAIL_PRIMARY_COLOR:#0f766e}
app.mail.brand.secondary-color=${MAIL_SECONDARY_COLOR:#0f172a}
app.mail.brand.accent-color=${MAIL_ACCENT_COLOR:#f59e0b}

# Allow large media payloads
spring.servlet.multipart.max-file-size=1024MB
spring.servlet.multipart.max-request-size=1100MB
server.tomcat.max-swallow-size=1100MB
server.tomcat.max-http-form-post-size=1100MB

# aws running

# ssh -i ~/SpringBootApp.pem ec2-user@13.201.88.149
# java -jar revconnect.jar

# Run backend permanently

# nohup java -jar revconnect.jar > log.txt 2>&1 &

# Check logs anytime
# tail -f log.txt
